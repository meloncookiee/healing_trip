# Healing Trip Gyeonggi — 작업 목록

근거: `docs/PRD.md`, `docs/DEVELOPMENT_PLAN.md`  
규칙: 한 번에 하나의 체크박스만 진행하고, 완료 시 `[x]`로 바꾼다.  
제외: 프로젝트 생성, conda/환경 세팅, scaffold

---

## MVP

### A. 백엔드 기반

- [x] `.env`에 템플스테이·관광정보 API 키/베이스 URL 설정값 정리 (코드 하드코딩 금지)
- [x] `Temple` SQLite 모델/테이블 정의
- [x] `Attraction` SQLite 모델/테이블 정의
- [x] DB 연결·초기화 유틸 정리
- [x] FastAPI 앱 기동 및 CORS 등 프론트 연동 최소 설정 확인

### B. 공공데이터 수집·저장

- [x] 경기도 템플스테이 API 클라이언트 작성
- [x] 템플스테이 응답 → `Temple` 스키마 매핑
- [x] 템플스테이 데이터를 SQLite에 저장(동기화)하는 로직 구현
- [x] 관광정보 API 클라이언트 작성
- [x] 관광정보 응답 → `Attraction` 스키마 매핑 (이미지·좌표 포함)
- [x] 관광지 데이터를 SQLite에 저장(동기화)하는 로직 구현
- [x] 수집 실패 시 로그/에러 처리 확인

### C. 백엔드 조회 API

- [x] `GET /temples` 템플스테이 목록 API
- [x] `GET /temples/{id}` 템플스테이 상세 API
- [x] `GET /attractions` 관광지 목록 API
- [x] `GET /attractions` 카테고리(관광 유형) 쿼리 지원
- [x] `GET /attractions/{id}` 관광지 상세 API
- [x] `GET /search` 키워드 검색 API (사찰명·관광지명)
- [x] `GET /temples/{id}/nearby` 반경 N km 주변 관광지 API
- [x] 목록/검색 응답 스키마 일관화 및 빈 결과 처리

### D. 프론트엔드 MVP UI

- [x] 백엔드 전용 API 클라이언트 정리 (공공 API 직접 호출 금지)
- [x] 기본 라우팅 구성 (홈 / 템플스테이 / 관광지 / 검색 / 상세)
- [x] 템플스테이 목록 페이지 구현
- [x] 템플스테이 상세 페이지 구현 (소개·위치·운영/프로그램)
- [x] 관광지 목록 페이지 구현
- [x] 관광지 상세 페이지 구현 (설명·위치·이미지)
- [x] 키워드 검색 UI 연결
- [x] 관광 유형(카테고리) 필터 UI 연결
- [x] 템플스테이 상세에 주변 관광지 추천 섹션 연결
- [x] 로딩·에러·결과 없음 Fallback UI 적용
- [x] 모바일 우선 기본 레이아웃 점검

### E. MVP 통합 점검

- [x] 데이터 동기화 후 목록·상세·검색·주변 추천 E2E 확인
- [x] 네트워크상 프론트→FastAPI만 호출되는지 확인
- [x] MVP 범위 밖 기능(지역 필터·지도·즐겨찾기)이 들어가지 않았는지 확인

---

## 추가 개발 (MVP 이후)

지시가 있기 전까지 진행하지 않는다.

### F. 지역 필터

- [x] DB/모델의 지역 필드 정규화
- [x] 목록·검색 API에 `region` 필터 파라미터 추가
- [x] 프론트 지역 선택 UI 구현
- [x] 지역 필터가 목록·검색 결과에 반영되는지 확인

### G. 지도

- [x] API 응답 lat/lng 필드 안정화 (필요 시 지도용 엔드포인트)
- [x] 지도 SDK 연동
- [x] 지도에 템플스테이 마커 표시
- [ ] 지도에 관광지 마커 표시
- [ ] 현재 위치 표시
- [ ] 템플스테이 선택 시 주변 관광지 지도 연동
- [ ] 마커 다수 시 클러스터링 검토·적용

### I. 경기도 공연·문화행사

- [x] 공연 API 클라이언트·동기화·조회 API
- [x] 공연 목록·상세 UI 및 검색 연동
- [x] 네비게이션·홈 검색 범위에 공연 추가

### J. 사찰 상세 보강

- [x] 유튜브·인스타 링크
- [x] 예약가능 체험 프로그램 사진 탭
- [x] 예약 가능일 검색
- [x] 소개·안내·시설·갤러리·후기 탭
- [x] 연꽃 커서·클릭 잎 떨어짐 효과

### H. 즐겨찾기

- [ ] 즐겨찾기 저장용 백엔드 API (추가·삭제·목록)
- [ ] 목록/상세 즐겨찾기 토글 UI
- [ ] 즐겨찾기 목록 페이지
- [ ] 즐겨찾기 추가·삭제·조회 E2E 확인
